class NameCircleBackgroundProps {
  const NameCircleBackgroundProps(
      {required this.fullname, this.isPrimaryBackgroundColor = true});

  final bool isPrimaryBackgroundColor;
  final String fullname;
}
